# 006 — changes against 005

- **Cross-fade editing**: all three scene changes are now cross-dissolves with overlapping opacity fades on one continuous camera path; the dip-to-dark swap and the cut-out of the zoom punch are gone (owner: the film used to flow more, panels melting into each other).
- Camera glides are slower and softer (1.05–1.2 s, power1.inOut); the zoom into the rotate sticker resolves by gliding out (1.2 s) instead of swapping in the dark.
- Panels/titles fade in with a whisper of scale; sticker pops stay (they are the beat language).
- **Cleanup-first executed**: superseded media (old compositions, QA keyframes, sticker sets 002–005, old audio, both previous MP4s, v001 leftovers) deleted before authoring; 006 re-uses the existing 10-sticker set and 6 narration beats (0 new stickers).
- Repo refreshed: `.gitignore` now excludes generated compositions and QA keyframes (contact sheets stay), `exports/` carries only the current film.
